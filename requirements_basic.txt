# VoiceClone AI Spanish - Dependencias Básicas (Sin compilación)
# Para sistemas que tienen problemas con herramientas de build

# IMPORTANTE: Spanish-F5 se debe instalar manualmente:
# pip install git+https://github.com/jpgallegoar/Spanish-F5.git

# Interfaz gráfica
gradio>=4.0.0

# Machine Learning base (ya instalado con PyTorch)
torch>=2.0.0
transformers>=4.30.0
accelerate>=0.20.0
huggingface-hub>=0.15.0

# Procesamiento de datos básico
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# Utilidades
tqdm>=4.65.0
pyyaml>=6.0

# Audio básico (sin compilación compleja)
soundfile>=0.12.0
pydub>=0.25.0

# HuggingFace
datasets>=2.12.0

# Nota: librosa requiere compilación, se puede instalar manualmente si es necesario:
# pip install librosa --no-deps
